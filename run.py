from app import app

if __name__=='__main__':
  app.run()
# import the app instance and check if the script runs directly and then use the app.run() method to pass in the debug=True argument to allow us to run in debugger mode to track errors in our application.